const $=s=>document.querySelector(s);const esc=s=>String(s??'--').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){try{const r=await fetch('/api/results?t='+Date.now(),{cache:'no-store'}),d=await r.json();if(!r.ok||!d.success)throw 0;const live=d.live||{};$('#live').textContent='● LIVE';$('#big').textContent=live.result||'--';$('#set').textContent=live.set||'--';$('#value').textContent=live.value||'--';$('#updated').textContent=live.updated_at?'Updated '+new Date(String(live.updated_at).replace(' ','T')+'Z').toLocaleString('en-US',{timeZone:'Asia/Yangon'}):'Waiting for result';$('#grid').innerHTML=(d.results||[]).map(x=>`<div class="card"><span>◷ ${esc(x.result_time)}</span><b>${x.status==='published'?esc(x.result_number):'--'}</b></div>`).join('')}catch{$('#live').textContent='● OFFLINE'}}load();setInterval(load,5000);
