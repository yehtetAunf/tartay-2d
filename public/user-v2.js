const cards=document.getElementById('resultCards'),latest=document.getElementById('latestNumber'),updated=document.getElementById('updatedText');
async function loadResults(){try{const r=await fetch('/api/results'),d=await r.json();if(!d.success)throw 0;const rows=d.results||[];cards.innerHTML=rows.map(x=>`<article class="result-card"><div class="card-head"><div>Time</div><div>Set</div><div>Value</div><div>2D</div></div><div class="card-body"><div class="time">${esc(x.result_time)}</div><div class="set">${esc(x.set_value)}</div><div class="value">${esc(x.market_value)}</div><div class="number">${esc(x.result_number)}</div></div></article>`).join('');const published=rows.filter(x=>/^\d{2}$/.test(x.result_number));if(published.length){const last=published[published.length-1];latest.textContent=last.result_number;updated.textContent=formatDate(last.updated_at)}else{latest.textContent='--';updated.textContent='Waiting for result'}}catch{cards.innerHTML='<p>Results could not be loaded.</p>'}}
function formatDate(v){if(!v)return '--';const d=new Date(v.replace(' ','T')+'Z');return new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Yangon',year:'numeric',month:'2-digit',day:'2-digit',hour:'numeric',minute:'2-digit',second:'2-digit',hour12:true}).format(d)}function esc(v){return String(v??'--').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;')}loadResults();setInterval(loadResults,30000);
