v6.14
- User Home SET/VALUE display mapping swapped back as requested.
- SET column displays the previously VALUE-mapped feed; VALUE column displays the previously SET-mapped feed.
- 2D calculation rule unchanged.
- v6.13 scheduled round-time visibility fix retained.
