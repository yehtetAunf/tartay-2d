v6.2 visible SET/VALUE + Big 2D jump
- Latest Coinbase API market remains the baseline.
- Active round SET/VALUE visibly change every 5 seconds, like the supplied reference video.
- SET movement is small (cent-level); VALUE movement is larger so digits visibly change.
- Big 2D is recalculated every jump:
  SET last digit + VALUE last digit before decimal.
- Active row 2D stays -- until official result.
- Future rounds remain -- / -- / --.
- Released rows stay fixed.
